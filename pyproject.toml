[project]
name = "hr-analytics-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyter",
    "numpy",
    "pandas",
    "matplotlib",
    "seaborn",
    "sqlalchemy",
    "psycopg2",
    "python-dotenv"
]
